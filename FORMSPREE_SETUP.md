# 📧 إعداد Formspree لإرسال الإيميلات

## ما هو Formspree؟

Formspree هو خدمة مجانية لإرسال النماذج عبر الإيميل بدون الحاجة لإعداد SMTP أو خوادم. سهل الاستخدام ولا يحتاج إعداد معقد.

## خطوات الإعداد:

### 1. إنشاء حساب على Formspree

1. اذهب إلى [Formspree.io](https://formspree.io)
2. اضغط على **Sign Up** أو **Get Started**
3. سجل حساب جديد (يمكن استخدام Gmail)

### 2. إنشاء نموذج جديد

1. بعد تسجيل الدخول، اضغط على **New Form**
2. اختر **Email notifications**
3. أدخل الإيميل المستقبل: `talkhanahmed422@gmail.com`
4. اضغط **Create Form**

### 3. الحصول على Endpoint URL

بعد إنشاء النموذج، ستحصل على رابط مثل:
```
https://formspree.io/f/xxxxxxxxxxxx
```

**انسخ هذا الرابط** - ستحتاجه في الخطوة التالية.

### 4. تحديث الكود

افتح ملف `script.js` وابحث عن:

```javascript
const FORMSPREE_ENDPOINT = 'https://formspree.io/f/YOUR_FORMSPREE_ENDPOINT';
```

استبدل `YOUR_FORMSPREE_ENDPOINT` بالرابط الذي حصلت عليه من Formspree.

**مثال:**
```javascript
const FORMSPREE_ENDPOINT = 'https://formspree.io/f/xjvqkzpn';
```

### 5. التأكد من تفعيل Formspree

في نفس الملف، تأكد من أن:
```javascript
const USE_FORMSPREE = true;
```

### 6. إعداد الإيميل المستقبل

1. في Formspree Dashboard، اذهب إلى إعدادات النموذج
2. تأكد من أن **Email notifications** مفعّل
3. تأكد من أن الإيميل المستقبل هو: `talkhanahmed422@gmail.com`

## المميزات:

✅ **مجاني** - حتى 50 إرسال شهرياً في الخطة المجانية  
✅ **سهل** - لا يحتاج إعداد SMTP  
✅ **يدعم المرفقات** - يمكن رفع الصور والملفات  
✅ **آمن** - يحمي من البريد المزعج (Spam)  
✅ **يعمل مباشرة** - لا يحتاج إعادة نشر على Netlify  

## الخطط المتاحة:

### الخطة المجانية:
- 50 إرسال شهرياً
- إيميلات غير محدودة
- دعم المرفقات

### الخطة المدفوعة:
- إرسالات غير محدودة
- ميزات إضافية
- دعم فني

## اختبار النموذج:

1. افتح موقعك
2. املأ النموذج ببيانات تجريبية
3. اضغط **Submit**
4. تحقق من بريد `talkhanahmed422@gmail.com`

## استكشاف الأخطاء:

### الإيميل لا يصل؟
1. تحقق من أن `FORMSPREE_ENDPOINT` صحيح في `script.js`
2. تأكد من أن `USE_FORMSPREE = true`
3. تحقق من إعدادات النموذج في Formspree Dashboard
4. تأكد من أن الإيميل المستقبل صحيح في Formspree

### خطأ في الإرسال؟
- افتح Console (F12) لرؤية الخطأ
- تحقق من أن الرابط صحيح
- تأكد من أن النموذج مفعّل في Formspree

### تجاوز الحد المسموح؟
- في الخطة المجانية: 50 إرسال شهرياً
- يمكن ترقية الخطة أو الانتظار حتى الشهر القادم

## التبديل بين Formspree و Netlify Functions:

في ملف `script.js`، يمكنك التبديل بسهولة:

```javascript
// لاستخدام Formspree
const USE_FORMSPREE = true;

// لاستخدام Netlify Functions
const USE_FORMSPREE = false;
```

---

✅ بعد إتمام هذه الخطوات، ستصل جميع طلبات النموذج إلى `talkhanahmed422@gmail.com` عبر Formspree!

