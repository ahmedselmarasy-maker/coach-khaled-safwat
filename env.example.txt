# إعدادات SMTP لإرسال الإيميل
# انسخ هذا الملف إلى .env واملأ القيم

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
TO_EMAIL=your-email@gmail.com

# منفذ الخادم المحلي (اختياري)
PORT=8888

