# 📧 إعداد الإيميل على Netlify

## خطوات إعداد متغيرات البيئة على Netlify

### 1. اذهب إلى Netlify Dashboard
- افتح [Netlify](https://app.netlify.com)
- سجل الدخول إلى حسابك
- اختر الموقع (Site) الخاص بك

### 2. إعداد متغيرات البيئة (Environment Variables)

1. من القائمة الجانبية، اختر **Site settings**
2. اضغط على **Environment variables** من القائمة
3. اضغط على **Add a variable** لإضافة كل متغير

### 3. أضف المتغيرات التالية:

#### المتغيرات المطلوبة:

| المتغير | القيمة |
|---------|--------|
| `SMTP_HOST` | `smtp.gmail.com` |
| `SMTP_PORT` | `587` |
| `SMTP_USER` | `your-email@gmail.com` (ضع بريدك الإلكتروني) |
| `SMTP_PASS` | `your-app-password` (ضع كلمة مرور التطبيق) |
| `TO_EMAIL` | `talkhanahmed422@gmail.com` |

### 4. إعداد Gmail لإرسال الإيميلات

#### الخطوة 1: تفعيل التحقق بخطوتين
1. اذهب إلى [Google Account](https://myaccount.google.com/)
2. اختر **Security** من القائمة
3. فعّل **2-Step Verification**

#### الخطوة 2: إنشاء كلمة مرور التطبيق
1. اذهب إلى [App Passwords](https://myaccount.google.com/apppasswords)
2. اختر **App** واكتب "Netlify" أو أي اسم
3. اضغط **Generate**
4. انسخ كلمة المرور التي ستظهر (16 حرف بدون مسافات)
5. ضعها في `SMTP_PASS` على Netlify

### 5. إعادة النشر (Redeploy)

بعد إضافة المتغيرات:
1. اذهب إلى **Deploys** من القائمة
2. اضغط على **Trigger deploy** → **Clear cache and deploy site**
3. انتظر حتى ينتهي النشر

### 6. اختبار الإعداد

1. افتح موقعك على Netlify
2. املأ النموذج ببيانات تجريبية
3. اضغط **Submit**
4. تحقق من بريد `talkhanahmed422@gmail.com`

## ملاحظات مهمة:

⚠️ **استخدم كلمة مرور التطبيق وليس كلمة المرور العادية**
- كلمة مرور التطبيق هي 16 حرف بدون مسافات
- يجب تفعيل التحقق بخطوتين أولاً

⚠️ **تأكد من صحة القيم:**
- `TO_EMAIL` يجب أن يكون `talkhanahmed422@gmail.com`
- `SMTP_USER` يجب أن يكون بريد Gmail الذي لديه كلمة مرور التطبيق
- `SMTP_PASS` يجب أن تكون كلمة مرور التطبيق (16 حرف)

## استكشاف الأخطاء:

### الإيميل لا يصل؟
1. تحقق من أن جميع المتغيرات موجودة
2. تأكد من صحة كلمة مرور التطبيق
3. تحقق من أن التحقق بخطوتين مفعّل
4. راجع سجلات Netlify Functions في **Functions** → **send-email**

### خطأ في الإرسال؟
- تحقق من سجلات Netlify Functions
- تأكد من أن `SMTP_USER` و `SMTP_PASS` صحيحين
- تأكد من أن `TO_EMAIL` مضبوط على `talkhanahmed422@gmail.com`

---

✅ بعد إتمام هذه الخطوات، ستصل جميع طلبات النموذج إلى `talkhanahmed422@gmail.com`

