# 🚀 تشغيل التطبيق محلياً

## الخطوات:

### 1. تثبيت المكتبات المطلوبة
```bash
npm install
```

### 2. إنشاء ملف `.env`
أنشئ ملف `.env` في المجلد الرئيسي واملأه بالمعلومات التالية:

```
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
TO_EMAIL=your-email@gmail.com
PORT=8888
```

**ملاحظة:** إذا كنت تستخدم Gmail:
- فعّل التحقق بخطوتين
- أنشئ [كلمة مرور التطبيق](https://myaccount.google.com/apppasswords)
- استخدم كلمة مرور التطبيق في `SMTP_PASS` (وليس كلمة المرور العادية)

### 3. تشغيل الخادم
```bash
npm start
```

أو:
```bash
node server.js
```

### 4. فتح المتصفح
افتح المتصفح واذهب إلى:
```
http://localhost:8888
```

## اختبار التطبيق

1. املأ النموذج ببيانات تجريبية
2. أضف بيانات التمارين الأربعة
3. (اختياري) ارفع صورة أو ملف
4. اضغط على "إرسال النموذج"
5. تحقق من بريدك الإلكتروني

## استكشاف الأخطاء

- إذا ظهرت رسالة خطأ بخصوص SMTP، تأكد من:
  - صحة إعدادات SMTP في ملف `.env`
  - أن كلمة مرور التطبيق صحيحة (لـ Gmail)
  - أن التحقق بخطوتين مفعّل (لـ Gmail)

- إذا لم يعمل الخادم:
  - تأكد من تثبيت جميع المكتبات: `npm install`
  - تأكد من أن المنفذ 8888 غير مستخدم

